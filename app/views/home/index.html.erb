Galpões & Estoque

<h2>Galpões</h2>
<% @warehouses.each do |w| %>
  <%= link_to(w.name, warehouse_path(id: w.id)) %>
  Código: <%= w.code %>
  Cidade: <%= w.city %>
  <%= "#{w.area} m²" %>
<%end%>


<% if @warehouses.empty? %>
  <p>Não existem galpões cadastrados</p>
<%end%>